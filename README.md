# Certificate Archive

这是一个可直接部署到 GitHub Pages 的静态证书展示页。

## 发布方式

1. 将整个文件夹内容上传到 GitHub 仓库。
2. 在仓库的 Settings → Pages 中选择 `Deploy from a branch`。
3. 选择 `main` 分支和 `/ (root)` 目录。
4. 发布后，用最终的 Pages 地址生成二维码。


